global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'sugang-app'
    scheme: https
    metrics_path: /actuator/prometheus
    # Render deployment target.
    # For local Spring Boot, switch this back to:
    # scheme: http
    # static_configs:
    #   - targets: ['host.docker.internal:8080']
    static_configs:
      - targets: ['sugang-5de3.onrender.com']
